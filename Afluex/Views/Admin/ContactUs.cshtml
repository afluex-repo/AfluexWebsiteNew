@model Afluex.Models.Blog

@{
    ViewBag.Title = "ContactUs";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<div class="row page-titles">
    <div class="col-md-5 align-self-center">
        <h3 class="text-primary">Career List</h3>
    </div>
    <div class="col-md-7 align-self-center">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0)">Enquiry</a></li>
            <li class="breadcrumb-item active"> Contact Us</li>
        </ol>
    </div>
</div>

@using (Html.BeginForm("AllRequest", "Admin", FormMethod.Post, new { @id = "form1" }))
{
    <div class="container-fluid">

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">


                        <br />
                        <div class="table-responsive m-t-10">
                            <table id="example23" class="display nowrap table table-hover table-striped table-bordered" cellspacing="0" width="100%">
                                <thead>
                                    <tr>
                                        <th>Sr No.</th>
                                        <th>Name</th>
                                        <th>About</th>
                                        <th>Mobile</th>
                                        <th>Email</th>
                                        <th>Date</th>
                                        <th>Message</th>
                                        <th>ContactusType</th>

                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null)
                                    {
                                        if (Model.List != null)
                                        {
                                            int i = 1;
                                            foreach (var Item in Model.List)
                                            {
                                                <tr class="text-left">
                                                    <td>
                                                        @i
                                                    </td>
                                                    <td>@Item.FullName</td>
                                                    <td>@Item.About</td>
                                                    <td>@Item.Mobile</td>
                                                    <td>@Item.Email</td>
                                                    <td>@Item.AddedOn</td>
                                                    <td>@Item.Message</td>
                                                    <td>
                                                       @Item.ContactusType
                                                    </td>

                                                </tr>
                                                i++;
                                            }
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}





